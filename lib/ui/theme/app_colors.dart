import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF000000); // Dark Mode Totale
  static const Color surface = Color(0xFF121212);    // Grigio scurissimo
  static const Color accent = Color(0xFF00FF00);     // Verde segnale
  static const Color textMain = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E8E8E);
}